# =========================================================
# Configuración General de la Aplicación
# =========================================================
spring.application.name=back-end
api.path=/
jwt.secret=$!@#$^%$$$%####$DDCPN0234FCFDPD8670M

# =========================================================
# Configuración de Recursos Estáticos (CSS, JS, Imágenes)
# =========================================================
# Indica a Spring dónde buscar activos estáticos que se sirven directamente
spring.web.resources.static-locations=classpath:/static/

# =========================================================
# Configuración de Thymeleaf (Motor de Plantillas)
# =========================================================
# Directorio base para las plantillas HTML
spring.thymeleaf.prefix=classpath:/templates/
# Extensión automática para los nombres de vista devueltos por el controlador
spring.thymeleaf.suffix=.html
# Modo de renderizado para asegurar compatibilidad con HTML5
spring.thymeleaf.mode=HTML
# Codificación de caracteres para soportar tildes y caracteres especiales
spring.thymeleaf.encoding=UTF-8
# Desactivar caché para ver cambios en caliente durante el desarrollo
spring.thymeleaf.cache=false

# =========================================================
# Configuración de Base de Datos MySQL (CMS)
# =========================================================
spring.datasource.url=jdbc:mysql://172.21.206.37:3306/cms?usessl=false
spring.datasource.username=root
spring.datasource.password=G0wLu8oWMqd2SDTDv1iy5dt9

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# =========================================================
# Configuración de MongoDB (Prescriptions)
# =========================================================
spring.data.mongodb.uri=mongodb://root:aWFMeIaddIne2pV84MMRRMhJ@172.21.114.100:27017/prescriptions?authSource=admin

# =========================================================
# Monitoreo y Salud (Actuator)
# =========================================================
management.endpoint.health.show-details=always
management.health.db.enabled=true
